{
  "name": "summarize",
  "version": "0.0.1",
  "description": "Subagent cartridge: takes long text, returns a short bullet summary via cloud LLM proxy. Reference implementation for the v0.5 subagent pattern (refinement §2.6).",
  "allowed_opcodes": ["call"],
  "preferred_tier": "auto",
  "subagent": true,
  "methods": {
    "summarize": {
      "args_schema": "schemas/summarize.args.schema.json",
      "description": "Compress text into at most N bullet points. Args: text (string), max_bullets (int 1-20, default 5)."
    }
  }
}
