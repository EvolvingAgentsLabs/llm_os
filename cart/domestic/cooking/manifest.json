{
  "name": "cooking",
  "version": "0.0.1",
  "description": "Meal planning, shopping-list generation, recipe authoring. Ported from skillos_mini/cartridges/cooking/. Method bodies stubbed in v0.01; real handlers land in v0.1 alongside the kernel fine-tune.",
  "allowed_opcodes": ["call"],
  "preferred_tier": "auto",
  "methods": {
    "plan_menu": {
      "args_schema": "schemas/plan_menu.args.schema.json",
      "description": "Produce a 7-day menu given household size and optional dietary notes."
    },
    "build_shopping_list": {
      "args_schema": "schemas/build_shopping_list.args.schema.json",
      "description": "Aggregate a weekly menu into an aisle-grouped shopping list."
    },
    "write_recipe": {
      "args_schema": "schemas/write_recipe.args.schema.json",
      "description": "Author a recipe for a single meal slot (breakfast/lunch/dinner)."
    }
  },
  "post_validators": ["menu_complete"]
}
